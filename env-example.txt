# AI Provider Configuration
# Default provider is "openrouter", can be set to "gemini" to use Google Gemini instead
AI_PROVIDER=openrouter

# OpenRouter API Key (required when AI_PROVIDER=openrouter)
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key

# Google Gemini API Key (required when AI_PROVIDER=gemini)
# Keep this for future use - Gemini code is preserved in the project
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key

# Optional: App URL for OpenRouter requests
NEXT_PUBLIC_APP_URL=http://localhost:3000
